class_name SubMapGenerator
extends RefCounted

# Placeholder interface for the full guidance architecture.
# The existing LocalMapGenerator remains the runtime local-map implementation for now.
func generate_sub_map(_tile_x: int, _tile_y: int):
	return null
