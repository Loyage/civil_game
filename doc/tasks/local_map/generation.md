# local_map 生成设计计划

## 目标

小地图生成器需要在不提前生成所有地块内部结构的前提下，保证任意地块第一次进入时都能生成稳定、可复现、与邻居衔接合理的 `256 x 256` 小地图。

## 生成输入

- 世界种子 `world_seed`
- 大地图地块坐标 `tile_col/tile_row`
- 大地图地块状态 `TileState`
- 大地图相邻地块信息
- 大地图河流方向 `river_flow`
- 大地图河流路径点 `river_path_points`
- 大地图山脉脊线路径点 `ridge_path_points`

## 种子策略

首版规划使用：

```text
local_seed = hash(world_seed, tile_col, tile_row)
```

用途划分：

- 全局高度连续性：主要依赖全局地格坐标采样，不能只依赖单地块 seed
- 局部细节扰动：使用 `local_seed`
- 地貌派生随机性：使用 `local_seed` 加不同 salt

## 坐标策略

小地图内部坐标：

```text
cell_x: 0..255
cell_y: 0..255
```

全局地格坐标实际实现为共享边界步长：

```text
global_cell_x = tile_col * 255 + cell_x
global_cell_y = tile_row * 255 + cell_y
```

原因是 `256 x 256` 小地图的边界地格需要被相邻地块复用：A 地块 `cell(255, y)` 与右侧 B 地块 `cell(0, y)` 必须采样同一个全局坐标。使用 `255` 步长可以保证共享边界高度逐格相等。

实现上，共享边界只使用全局高度场。大地图地块海拔、山脉、水体等宏观约束通过 `edge_weight` 从边界向内部渐入，避免相邻地块因为各自宏观属性不同而破坏边界高度一致性。河流可以在边界设置 `river_flags`，但不会改写最外圈高度。

## 高度范围

小地图每个地格高度是整数：

```text
-256..256
```

大地图地块海拔后续也改为同一范围的整数。当前大地图仍使用 `0..1` float，重构时需要从阈值、渲染和 UI 显示三处同步修改。

## 高度生成思路

首版生成分层：

1. 全局低频高度场：决定大陆、海岸和大地形趋势。
2. 大地图地块约束：让小地图平均高度接近当前大地图地块海拔对应的目标高度。
3. 局部高频扰动：生成细节起伏。
4. 山脉约束：如果大地图地块有山脉或山脉脊线，则沿 `ridge_path_points` 抬高高度。
5. 河流约束：如果大地图地块有河流，则根据入口/出口生成河道，并压低河床高度。

## 河流生成

大地图指定河流方向，小地图内部路径独立寻路：

- 入口和出口由大地图 `river_flow` 和邻接关系决定
- 如果存在上游邻居，则入口位置来自共享边界
- 出口位置根据 `river_flow` 映射到对应边界
- 小地图内部使用基础高度成本寻路，优先沿低处、缓坡通行
- 河床高度需要低于周围地格，保证连接处通畅
- 湖泊地块允许河流进入低洼水体后停止

## 水体规则

首版采用：

```text
height < 0 => water
```

后续可以引入 `sea_level` 配置，但首版默认海平面为 `0`。

## 大地图海拔关系

当前不立即强制大地图海拔等于小地图平均值。首版策略：

- 大地图先按现有逻辑生成宏观信息
- 小地图生成时尽量匹配对应地块的大地图海拔、山脉、河流和水体信息
- 生成后计算 `average_height`
- 后续重构时再用小地图平均值校验或调整大地图地块海拔

## Checklist

- [x] 设计 `world_seed + tile_col/tile_row` hash 规则
- [x] 设计全局地格坐标采样函数
- [x] 设计 `0..1` 大地图海拔到 `-256..256` 的临时映射
- [x] 设计小地图高度生成层级
- [ ] 设计山脉脊线抬高规则
- [x] 设计河流入口/出口映射规则
- [x] 设计小地图河流寻路成本
- [x] 设计河床压低规则
- [x] 设计水体派生规则
- [x] 设计平均高度统计规则
